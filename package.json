{
  "name": "musicmap",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "install-dependencies": "npm run install-music-map && npm install",
    "setup-production": "npm run install-music-map && npm run build-music-map && npm install",
    "install-music-map": "cd music-map && npm install",
    "build-music-map": "cd music-map && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "nodemon": "^2.0.22"
  }
}
